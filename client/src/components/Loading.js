const Loading = () => {
  return (
    <div
      style={{
        height: "100%",
        minHeight: "100vh",
        width: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: "transparent",
      }}
    >
      <img src="m2.gif" alt="mario" width={160} />
    </div>
  );
};

export default Loading;
